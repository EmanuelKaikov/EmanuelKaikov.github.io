# מדריך העלאת האתר לרשת וחיבור דומיין אישי (PromIT)

האתר של **PromIT** נבנה בטכנולוגיות המתקדמות ביותר, בעיצוב הוליוודי יוקרתי, רספונסיבי לחלוטין ומותאם באופן מושלם לכל מסך ולכל הדפדפנים.

---

## 🌟 האפשרות המומלצת ביותר: אחסון חינמי ב-Vercel / Netlify / Cloudflare Pages

במקום לשלם על שרתים יקרים או להתעסק בתחזוקת שרת, מומלץ להשתמש בשרות ענן מודרני כמו **Vercel** או **Netlify**.

### ⚡ למה זה "הרבה יותר טוב"?
1. **100% חינם לתמיד** (כולל תעודת אבטחה SSL/HTTPS אוטומטית).
2. **מהירות שיא (Global CDN)** – האתר נטען תוך מילישניות בכל מקום בארץ ובעולם.
3. **חיבור דומיין אישי בלחיצת כפתור** (למשל `promit.co.il`).
4. **גיבוי ועדכונים אוטומטיים** בכל פעם שמשנים קוד.

---

## 🚀 דרך 1: העלאה מהירה ל-Vercel (מומלץ)

1. היכנסו ל-[Vercel.com](https://vercel.com) והירשמו בחינם (ניתן להירשם עם משתמש GitHub / Google).
2. התקינו את Vercel CLI במחשב (אופציונלי) או פשוט גררו את תיקיית `promit` למסך הדרג-אנד-דרופ באתר Vercel.
3. בלחיצת כפתור אחת האתר שלכם יהיה באוויר עם כתובת כמו: `promit.vercel.app`.

---

## 🌐 דרך 2: העלאה ל-GitHub Pages

1. פתחו ריפוזיטורי חדש ב-GitHub בשם `promit`.
2. העלו את הקבצים (`index.html`, `styles.css`, `script.js`, ותקיית `videos/`).
3. בהגדרות הריפו (Settings -> Pages) בחרו בענף `main` ושמרו.
4. תוך דקה האתר מפורסם בחינם!

---

## 🔗 איך לחבר דומיין אישי (כמו `promit.co.il` או `promit.com`)

אם רכשתם דומיין בספק ישראלי (כמו Box, LiveDNS, DomainTheNet, רשם הדומיינים הישראלי) או ב-GoDaddy / Namecheap:

### שלב 1: הגדרה בפאנל האחסון (Vercel / Netlify)
1. היכנסו להגדרות האתר שלכם ב-Vercel / Netlify -> **Domains**.
2. הקלידו את הדומיין שלכם: `promit.co.il` (ו-`www.promit.co.il`).
3. המערכת תציג לכם 2 רשומות DNS שעליכם להגדיר.

### שלב 2: הגדרת רשומות DNS בניהול הדומיין שלכם
היכנסו לפאנל הניהול של ספק הדומיין שלכם והוסיפו:

* **רשומת A (עבור הדומיין הראשי `promit.co.il`):**
  * Type: `A`
  * Name / Host: `@` (או להשאיר ריק)
  * Value / Target: `76.76.21.21` *(אם מדובר ב-Vercel)*

* **רשומת CNAME (עבור `www.promit.co.il`):**
  * Type: `CNAME`
  * Name / Host: `www`
  * Value / Target: `cname.vercel-dns.com` *(או הכתובת שמספקת Netlify/Vercel)*

4. תוך מספר דקות עד 24 שעות הדומיין יתחבר אוטומטית עם מנעול ירוק של אבטחת HTTPS!

---

## 📞 תמיכה ויצירת קשר
לכל שאלה או התייעצות לגבי חיבור הדומיין, **אוהד בקשי (PromIT)** זמין עבורכם:
* **טלפון / וואטסאפ:** 054-3534890
